\part{K-fold Cross Validation}

To tune the hyperparameter $k$, one technique is the k-fold cross-validation (k-fold = number of folds). 

\begin{enumerate}
    \item For each choice of neighbors $k$, estimate: 
    $$
    E_{\text{fold}}[\text{Accuracy(k)}]$$

    \item 
\end{enumerate}